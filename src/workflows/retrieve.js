/**
 * @intent retrieve
 * @description Retrieve information or records for the user.
 * @example "Retrieve my last 5 notes"
 */
export async function run({ prompt, context }) {
  // TODO: Implement retrieval logic
  return {
    message: "Information retrieved (mock)",
    prompt,
  };
} 