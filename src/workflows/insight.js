/**
 * @intent insight
 * @description Generate insights from user data or input.
 * @example "Give me insights about my recent meetings"
 */
export async function run({ prompt, context }) {
  // TODO: Implement insight logic
  return {
    message: "Insight generated (mock)",
    prompt,
  };
} 