/**
 * @intent summarize
 * @description Summarize content, meetings, or notes for the user.
 * @example "Summarize today's meeting notes"
 */
export async function run({ prompt, context }) {
  // TODO: Implement summarization logic
  return {
    message: "Summary generated (mock)",
    prompt,
  };
} 