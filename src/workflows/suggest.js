/**
 * @intent suggest
 * @description Suggest actions, content, or improvements to the user.
 * @example "Suggest a meeting time for next week"
 */
export async function run({ prompt, context }) {
  // TODO: Implement suggestion logic
  return {
    message: "Suggestion provided (mock)",
    prompt,
  };
} 